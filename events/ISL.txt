add_namespace = emirate

# War with Israelits
country_event = {
	id = emirate.1
	title = emirate.1.t
	desc = emirate.1.d
	picture = GFX_report_event_SHU_emirate_at_war
	
	is_triggered_only = yes
	
	option = {
		name = emirate.1.a
		declare_war_on = {
            target = JEW
            type = annex_everything
        }
		SHU = {
		country_event = {
	        id = shua.8
			days = 3
		}
		}
		hidden_effect = {
		    news_event = { id = news.12 }
		}
	}
}